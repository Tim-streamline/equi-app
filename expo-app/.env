# Reach the host's loopback from the Android emulator. iOS simulators can
# use http://localhost directly. For a physical device, set this to the
# host machine's LAN IP and the backend's exposed APP_PORT.
EXPO_PUBLIC_API_BASE_URL=http://192.168.1.209:81
